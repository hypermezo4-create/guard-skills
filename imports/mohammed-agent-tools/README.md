# Mohammed Agent Tools — 358-skill import

This import comes from the user-provided `Mohammed_AI_Agent_Tools(1).zip` bundle.

- Skills discovered: **358**
- Skill payload files: **1,678**
- Materialized destination: `.agents/skills/<skill>/`
- Catalog: `catalog.json`
- Compressed source payload: `skills.tar.xz.b64`
- Integrity: SHA-256 in `bundle.sha256`

The bundle is stored compactly so GitHub Actions can materialize every skill file into the repository in one verified operation. The original per-skill provenance and license files are preserved. Imported third-party skills are not labeled as Mohammed-authored or reviewed merely because they are hosted here.

Run locally:

```bash
bash scripts/materialize-agent-tools.sh
```
